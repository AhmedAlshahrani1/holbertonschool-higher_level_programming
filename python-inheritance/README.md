# Python - Inheritance

## Description
This project introduces the concepts of inheritance in Python Object-Oriented Programming (OOP). It covers how to create subclasses, inherit attributes and methods from base classes, override methods, and understand multiple inheritance.

## Learning Objectives
By the end of this project, I am able to explain:
* What is a superclass, baseclass, or parentclass.
* What is a subclass.
* How to list all attributes and methods of a class or instance.
* How to inherit a class from another.
* How to override a method or attribute inherited from the base class.
* The purpose of inheritance.
* How to use `isinstance`, `issubclass`, `type`, and `super` built-in functions.

## Requirements
* **OS:** Ubuntu 20.04 LTS
* **Language:** Python 3.8.5
* **Style Guidelines:** pycodestyle (version 2.7.*)
* All files end with a new line.
* The first line of all files is exactly `#!/usr/bin/python3`.
* All files must be executable.
* All test files are inside a `tests` folder and executed using the `doctest` module.

## Project Files
| File Name | Description |
|-----------|-------------|
| `0-lookup.py` | A function that returns the list of available attributes and methods of an object. |
| `1-my_list.py` | A class `MyList` that inherits from `list` and prints the list sorted. |
| `tests/` | Directory containing all text files for doctests. |
*(Note: Update the table above with the actual file names and descriptions as you complete the project tasks).*

## Author
* Ahmed Alhatim
